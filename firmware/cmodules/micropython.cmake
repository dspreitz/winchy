# Winchy user C modules, compiled INTO the MicroPython firmware via
# USER_C_MODULES (see firmware/fwbuild/build.sh). Each module lives in its own
# subdirectory with its own micropython.cmake.
include(${CMAKE_CURRENT_LIST_DIR}/winchy_fast/micropython.cmake)
