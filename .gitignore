__pycache__/
*.pyc
*.log

# Range-test log artifacts (device dumps / captures, not source)
range_log_*.txt
*_walk*.csv
winch_rxlog*.csv
raw*.csv

# WiFi credentials live only on the device, never in the repo
secrets.py
